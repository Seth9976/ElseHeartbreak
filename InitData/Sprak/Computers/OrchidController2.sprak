# By Fib, 18 Jan 1995

string baseName = "DorisGardens_West_Orchid1_"
number NR_OF_ORCHIDS = 12

loop
    loop from 1 to NR_OF_ORCHIDS
        TurnOn(baseName + @)
    end
    Sleep(1)
    loop from 1 to NR_OF_ORCHIDS
        TurnOff(baseName + @)
    end
    Sleep(1)
end
